"""
A module that provides access to databases (vector and external database for LLM)
`external_db` and `vector_db` - for connect to LLM
`local_connect_data` - Local data for connecting databases
`_config` - sql queries for database initialization
"""
